---
title: 認証API
description: 認証エンドポイントとメソッド
order: 23
---

すべての認証エンドポイントは `/api/auth/` 以下にあります。

## 概要

Bedrudは複数の認証方式に対応しています：

| 方式 | エンドポイント | 説明 |
|--------|----------|-------------|
| Email/Password | `POST /api/auth/login` | 従来のログイン |
| Registration | `POST /api/auth/register` | アカウント作成 |
| Guest | `POST /api/auth/guest-login` | 一時アクセス |
| OAuth | `GET /api/auth/:provider/login` | ソーシャルログイン |
| Passkeys | `POST /api/auth/passkey/*` | FIDO2/WebAuthn |
| Token Refresh | `POST /api/auth/refresh` | アクセストークンの更新 |

| Resend Verification | `POST /api/auth/verify/resend` | Resend verification email |
| Verify Email | `GET /api/auth/verify` | Verify email with token (query param, redirect) |
| Check Verification Status | `GET /api/auth/verify/status` | Check if email is verified |
| Forgot Password | `POST /api/auth/forgot-password` | Request password reset email |
| Reset Password | `POST /api/auth/reset-password` | Reset password with token |

## トークン形式

認証に成功すると、JWTトークンのペアが返されます：

```json
{
  "accessToken": "eyJhbGciOiJIUzI1NiIs...",
  "refreshToken": "eyJhbGciOiJIUzI1NiIs..."
}
```

- **Access Token** - 有効期間が短く、`Authorization` ヘッダーで使用します
- **Refresh Token** - 有効期間が長く、新しいアクセストークンを取得するために使用します

### トークンの使用

認証が必要なすべてのリクエストにアクセストークンを含めてください：

```
Authorization: Bearer <accessToken>
```

---

## エンドポイント

### アカウント作成

新しいユーザーアカウントを作成します。

```
POST /api/auth/register
```

**リクエストボディ：**

```json
{
  "email": "user@example.com",
  "password": "securepassword",
  "name": "John Doe"
}
```

**レスポンス (200)：**

```json
{
  "accessToken": "eyJ...",
  "refreshToken": "eyJ...",
  "user": {
    "id": "uuid",
    "email": "user@example.com",
    "name": "John Doe",
    "role": "user"
  }
}
```

---

### ログイン

メールアドレスとパスワードで認証します。

```
POST /api/auth/login
```

**リクエストボディ：**

```json
{
  "email": "user@example.com",
  "password": "securepassword"
}
```

**レスポンス (200)：**

```json
{
  "accessToken": "eyJ...",
  "refreshToken": "eyJ...",
  "user": {
    "id": "uuid",
    "email": "user@example.com",
    "name": "John Doe",
    "role": "user"
  }
}
```

---

### ゲストログイン

アカウントを作成せずにゲストとして参加します。ゲストには制限付きの権限が付与されます。

```
POST /api/auth/guest-login
```

**リクエストボディ：**

```json
{
  "name": "Guest User"
}
```

**レスポンス (200)：**

```json
{
  "accessToken": "eyJ...",
  "refreshToken": "eyJ...",
  "user": {
    "id": "uuid",
    "name": "Guest User",
    "role": "guest"
  }
}
```

---

### 現在のユーザー情報の取得

認証済みユーザーのプロフィールを取得します。

```
GET /api/auth/me
```

**ヘッダー：** `Authorization: Bearer <accessToken>`

**レスポンス (200)：**

```json
{
  "id": "uuid",
  "email": "user@example.com",
  "name": "John Doe",
  "avatar": "https://...",
  "role": "user",
  "provider": "email"
}
```

---

### トークンのリフレッシュ

リフレッシュトークンを新しいアクセストークンと交換します。

```
POST /api/auth/refresh
```

**リクエストボディ：**

```json
{
  "refreshToken": "eyJ..."
}
```

**レスポンス (200)：**

```json
{
  "accessToken": "eyJ...",
  "refreshToken": "eyJ..."
}
```

---

### ログアウト

現在のリフレッシュトークンを無効化します。

```
POST /api/auth/logout
```

**ヘッダー：** `Authorization: Bearer <accessToken>`

**リクエストボディ：**

```json
{
  "refreshToken": "eyJ..."
}
```

**レスポンス (200)：**

```json
{
  "message": "logged out"
}
```

---

### OAuthログイン

ソーシャルプロバイダーでOAuthフローを開始します。

```
GET /api/auth/:provider/login
```

**対応プロバイダー：**

| プロバイダー | パス |
|----------|------|
| Google | `/api/auth/google/login` |
| GitHub | `/api/auth/github/login` |
| Twitter | `/api/auth/twitter/login` |

サーバーはユーザーをプロバイダーの認証ページにリダイレクトします。認証後、プロバイダーはコールバックURLにリダイレクトし、サーバーがJWTトークンを返します。

---

### 確認メールを再送信

確認メールのリンクを再送信します。未確認のローカルアカウントのみ対象。`verificationEmailCooldownMins` によってクールダウンを静かに適用 — メール列挙を防ぐため常に200を返します。

---

### メールを確認

確認リンクのトークンを使用してメール確認を完了します。

---

### パスワードを忘れた場合

パスワードリセットメールをリクエストします。SMTPの設定が必要です。

エンドポイントはメール列挙を防ぐため常に202を返します。リセットリンクは `resetTokenTTLHours` 後に期限切れになります。

---

### パスワードをリセット

リセットメールのトークンを使用して新しいパスワードを設定します。

---

## 
すべての認証エンドポイントは以下の形式でエラーを返します：

```json
{
  "error": "invalid credentials"
}
```

| ステータスコード | 意味 |
|--------|---------|
| 400 | 不正なリクエスト（フィールド不足、バリデーションエラー） |
| 401 | 認証情報が無効、またはトークンの有効期限切れ |
| 403 | Email not verified (`requiresVerification: true`) |
| 429 | Rate limited — check `Retry-After` header |
| 409 | メールアドレスはすでに登録されています |
| 500 | サーバー内部エラー |

---

## 関連ドキュメント

- [/ja/docs/backend/authentication](/ja/docs/backend/authentication) - how JWT, OAuth, passkeys, email verification, and password reset work internally
- [Passkeys API](/ja/docs/api/passkeys) - FIDO2/WebAuthnエンドポイントリファレンス
