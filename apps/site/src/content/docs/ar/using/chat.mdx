---
title: الدردشة داخل الاجتماع
description: رسائل نصية وتفاعلات الرموز واستطلاعات ومرفقات صور
order: 7
---

الدردشة داخل الغرفة لوحة جانبية للنص والتفاعلات والاستطلاعات والصور دون مغادرة المكالمة.

## الفتح والإرسال

1. افتح لوحة **الدردشة** من عناصر تحكم الاجتماع.
2. أرسل رسالة نصية — يستلمها المشاركون الآخرون.
3. فتح اللوحة أو إغلاقها **يجب ألا** يقطع مسارات الكاميرا أو الميكروفون.

يجب أن تبدو تجميع الرسائل والطوابع الزمنية صحيحة حتى مع الإرسال المتتابع.

## الرموز التعبيرية

- **تفاعل** على رسالة — يحدّث الطرفان أعداد التفاعل.
- **منتقي الرموز** في المُحرر يدرج الرمز في مسودة النص.

## الاستطلاعات

1. أنشئ **استطلاعًا** من الدردشة.
2. يرى الجميع فقاعة الاستطلاع.
3. **يصوّت** المشاركون؛ تتحدّث النتائج (نافذة نتائج إن وفرتها الواجهة).

## الصور والملفات

- أرفق صورة ضمن حدود الحجم والأبعاد على الخادم.
- يُجري عميل الاجتماع **فحصًا مسبقًا** لحجم الملف وأبعاد الصورة قبل الرفع باستخدام الإعدادات العامة (`chatUploadMaxBytes`، `chatUploadMaxDimension`).
- تُرفض الملفات الكبيرة جدًا أو الأبعاد الزائدة برسالة خطأ واضحة (العميل أولًا؛ الخادم يبقى المرجع — `413` / `400` على `POST /api/room/:roomId/chat/upload`).
- يمكن للمستلمين فتح الصور في **lightbox**.

**الافتراضيات:** 10 MiB لكل ملف، وأقصى أبعاد 8192×8192 (عند عدم التعيين).

يضبط المشغّلون الحدود عبر:

- `chat.uploads.maxBytes` / `chat.uploads.maxDimension` في [الإعدادات](/ar/docs/getting-started/configuration)
- تبويب **Chat Uploads** للمسؤول (`chatUploadMaxBytes`، `chatUploadMaxDimension`) في [لوحة الإدارة](/ar/docs/guides/admin-dashboard)
- حصص التخزين لكل مستخدم / عامة تحت `chat` (`maxUploadBytesPerUser`، خلفيات disk/S3)


## الاحتفاظ (من جانب العميل)

لا يحفظ LiveKit دردشة قناة البيانات على الخادم كما يفعل منتج دردشة. يطبّق Bedrud إشارات احتفاظ **من جانب العميل**:

| الإعداد | الافتراضي (نموذجي) | المعنى |
|---------|---------------------|--------|
| `chat.maxMessageCount` | 10000 | سقف الرسائل في الذاكرة / تخزين الجلسة |
| `chat.messageTTLHours` | 2160 (٩٠ يومًا) | تقليم حسب العمر |

تجاوزات البيئة: `CHAT_MAX_MESSAGE_COUNT`، `CHAT_MESSAGE_TTL_HOURS`.

بعد **إعادة التحميل وسط الغرفة** يتبع السجل سياسة المنتج تلك (استعادة جلسة أو بداية نظيفة) دون تعطّل.

## ذات صلة

- [عناصر تحكم الاجتماع](/ar/docs/using/meeting-controls)
- [الانضمام إلى اجتماع](/ar/docs/using/joining-a-meeting)
- [معالجات API](/ar/docs/backend/api-handlers) — `POST /room/:roomId/chat/upload`
