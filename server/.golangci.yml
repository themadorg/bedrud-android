run:
  timeout: 5m
  modules-download-mode: readonly
  allow-parallel-runners: true

linters:
  disable-all: true
  enable:
    - govet
    - staticcheck
    - unused
    - errcheck
    - ineffassign
    - unconvert
    - goconst
    - bodyclose
    - sqlclosecheck
    - rowserrcheck
    - gocritic
    - revive
    - misspell
    # gofumpt disabled: gofumpt ≥0.9 with this Go toolchain (go1.26.4-X) misclassifies
    # stdlib imports (e.g. sorts "fmt" after "bedrud/..."), fighting golangci's embedded
    # gofumpt and making format non-convergent. Use make fmt (goimports) instead.
    # - gofumpt
    - thelper
    - testifylint
    - intrange
    - usestdlibvars
    - nolintlint

linters-settings:
  revive:
    rules:
      - name: exported
        disabled: true
  gocritic:
    enabled-tags:
      - diagnostic
      - style
      - performance
    disabled-checks:
      - appendAssign

issues:
  exclude-rules:
    - path: docs/
      linters:
        - all
    - path: frontend/
      linters:
        - all
    - path: magefile.go
      linters:
        - all
  max-issues-per-linter: 0
  max-same-issues: 0
